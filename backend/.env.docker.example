# Copy this file to backend/.env.docker and replace the placeholder secrets
# before running the production containers.

SECRET_KEY=replace-with-secure-random
PASSWORD_SALT=replace-with-salt
FERNET_KEY=replace-with-fernet-key
# The database file lives inside /app/data in the container,
# which is bind-mounted to ./data on the host.
DATABASE_URL=sqlite+aiosqlite:///./data/accessvault.db
# Update this to match the domain serving the SPA if needed.
CORS_ORIGINS=["http://localhost"]
